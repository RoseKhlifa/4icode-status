"use client";

/**
 * GroupTags stub (4i.codes 版本)
 *
 * 上游 check-cx 用 tags (英文逗号分隔) 给每个分组打标签。
 * 4i.codes 从 provider config 里派生的分组不带 tags,
 * 这个组件始终返回 null,不占位。
 */
export function GroupTags(_props: { tags?: string | null }) {
  void _props;
  return null;
}
