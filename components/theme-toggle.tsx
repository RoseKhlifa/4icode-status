"use client";

/**
 * ThemeToggle stub (4i.codes 版本)
 *
 * 4i.codes 只提供浅色主题,顶栏胶囊里已经有一个占位按钮。
 * dashboard-view.tsx 内部也有一处 ThemeToggle 引用,这里返回 null 让它消失。
 */
export function ThemeToggle() {
  return null;
}
